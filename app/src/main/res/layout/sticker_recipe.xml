<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ConstraintLayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#121212"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/sticker_img"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:id="@+id/sticker_recipe_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/sticker_img"
        app:layout_constraintStart_toStartOf="@+id/sticker_img"
        app:layout_constraintTop_toBottomOf="@+id/sticker_img"
        tools:text="Recipe Name" />

    <TextView
        android:id="@+id/sticker_serving_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:textColor="#ffffff"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/sticker_recipe_name"
        app:layout_constraintStart_toStartOf="@+id/sticker_recipe_name"
        app:layout_constraintTop_toBottomOf="@+id/sticker_recipe_name"
        tools:text="Serving Count" />
</androidx.constraintlayout.widget.ConstraintLayout>